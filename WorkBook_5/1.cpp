#include <iostream>
#include <string>
using namespace std;

int main()
{
    string line;
    string newLine;
    string current;
    int left = 0;
    getline(cin, line);
    for (char(i): line)
    {
        newLine = i + newLine;
    }
    cout << newLine;
}
